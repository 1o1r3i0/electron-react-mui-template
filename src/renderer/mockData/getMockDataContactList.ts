import Contact from '../classes/Contact';

function getMockDataContactList(): Contact[] {
  const contactList: Contact[] = [];
  // contactList.push(new Contact());
  // contactList.push(new Contact());
  // contactList.push(new Contact());
  // contactList.push(new Contact());
  // contactList.push(new Contact());
  // contactList.push(new Contact());
  // contactList.push(new Contact());
  // contactList.push(new Contact());
  // contactList.push(new Contact());
  // contactList.push(new Contact());
  // contactList.push(new Contact());
  // contactList.push(new Contact());
  // contactList.push(new Contact());
  // contactList.push(new Contact());
  // contactList.push(new Contact());
  // contactList.push(new Contact());

  return contactList;
}

export default getMockDataContactList;
